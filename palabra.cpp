#include "palabra.h"
#include <stdio.h>


struct nodo_palabra{
    Cadena palabra; 
};

//Creamos la estructura palabra
palabra CrearPalabra(){
    palabra p=new(nodo_palabra);
    p[MAX_CANT_PALABRAS_X_LINEA];

return p;
}